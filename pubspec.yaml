name: test_flutter_bloc
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_bloc: ^8.1.1
  shimmer: ^2.0.0
  http:
  share_plus: ^4.5.3


dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
 uses-material-design: true
